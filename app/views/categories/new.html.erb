<%= simple_form_for @category, :url => categories_path do |f| %>
	<%= render :partial => 'form', :locals => {:f => f }%>
	<span>
		<%= f.submit '创建', :class => 'btn btn-primary' %>
		<%= link_to '返回', categories_path, :class => 'btn' %>
	</span>
<% end %>
